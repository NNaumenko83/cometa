import { StyledSectionTitle } from './SectionTitle.styled'

/* eslint-disable react/prop-types */

const SectionTitle = ({ children }) => <StyledSectionTitle>{children}</StyledSectionTitle>

export default SectionTitle
